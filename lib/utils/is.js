export const isIDLE = (status) => status === undefined;

export const isLoading = (status) => status === 'loading';

export const isError = (status) => status === 'error';

export const isDone = (status) => status === 'done';
